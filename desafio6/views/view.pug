br
a(class='button', href="/api/nuevo-producto") Ingresar Producto

    if products 

        table(class="table")
            thead
                tr 
                    th(scope="col") ID
                    th(scope="col") Nombre
                    th(scope="col") Precio
                    th(scope="col") Imagen
            tbody


            each product in products 

                tr  
                    th(scope="row")= product.id
                    td= product.title
                    td= product.price
                    td
                        img(src=product.file, class="img-thumbnail")

    else 

        p No hay productos cargados